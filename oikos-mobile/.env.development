# Expo web / Metro dev server has no built-in proxy (unlike Vite for oikos-web),
# so this points straight at the local backend. The API's CORS config allows
# any localhost/127.0.0.1 origin (see SecurityConfiguration on oikos-api).
EXPO_PUBLIC_API_URL=http://localhost:8080/api/v1
