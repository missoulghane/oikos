server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # React Router client-side routing: any path not matching a static
    # asset falls back to index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
